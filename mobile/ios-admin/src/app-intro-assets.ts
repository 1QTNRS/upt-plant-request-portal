import type { ImageSourcePropType } from "react-native";

/** Same mark as the native splash (`assets/splash-icon.png`). */
export const APP_INTRO_SPLASH_ICON: ImageSourcePropType = require("../assets/splash-icon.png");
